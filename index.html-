<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Manju ❤️</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #ffebf0; /* Soft pink background */
            font-family: 'Arial', sans-serif;
            overflow: hidden;
        }

        /* Moving Tree - Positioned at the bottom so it doesn't block buttons */
        .tree {
            position: absolute;
            bottom: -20px;
            width: 350px;
            z-index: 1; /* Behind the content */
            animation: sway 5s infinite ease-in-out;
        }

        @keyframes sway {
            0%, 100% { transform: rotate(-2deg); }
            50% { transform: rotate(2deg); }
        }

        /* Butterflies flying around */
        .butterfly {
            position: absolute;
            width: 30px;
            z-index: 2;
            animation: fly 10s infinite linear;
        }

        @keyframes fly {
            0% { left: -50px; top: 80%; transform: rotate(0deg); }
            50% { left: 50%; top: 20%; transform: rotate(20deg); }
            100% { left: 110%; top: 50%; transform: rotate(-20deg); }
        }

        /* Main Card */
        .container {
            position: relative;
            z-index: 10; /* Always in front of the tree */
            background: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            border: 4px solid #ff4d6d;
            max-width: 90%;
        }

        h1 { color: #d63384; margin-bottom: 20px; font-size: 2.2rem; }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
            min-height: 60px;
        }

        button {
            padding: 15px 30px;
            font-size: 1.2rem;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
            transition: 0.3s;
        }

        #yesBtn { background: #ff4d6d; color: white; }
        
        #noBtn { 
            background: #888; 
            color: white; 
            position: absolute; /* Allows it to jump everywhere */
            z-index: 11; /* In front of everything else */
        }

        #successMessage { display: none; color: #d63384; font-size: 1.8rem; }
    </style>
</head>
<body>

    <img src="https://img.freepik.com/free-vector/spring-floral-tree-background_23-2148415432.jpg" class="tree" alt="Flowering Tree">

    <img src="https://pngimg.com/uploads/butterfly/butterfly_PNG1017.png" class="butterfly" style="animation-delay: 0s;">
    <img src="https://pngimg.com/uploads/butterfly/butterfly_PNG1017.png" class="butterfly" style="animation-delay: 5s;">

    <div class="container">
        <div id="content">
            <h1>Hey Manju! 👋<br>Will you be my<br>Friendship Valentine? 🌸</h1>
            <div class="btn-group">
                <button id="yesBtn">YES! 🥰</button>
                <button id="noBtn">No 🥺</button>
            </div>
        </div>
        <div id="successMessage">
            Yayy! Best Friends Forever! 💖✨<br>
            <i>"Kesariya tera ishq hai Piya..."</i> 🧡
        </div>
    </div>

    <audio id="bgMusic" loop>
        <source src="https://www.pagalworld.com.se/files/download/id/64630" type="audio/mpeg">
    </audio>

    <script>
        const noBtn = document.getElementById('noBtn');
        const yesBtn = document.getElementById('yesBtn');
        const music = document.getElementById('bgMusic');

        // Start music on first click
        document.body.addEventListener('click', () => {
            music.play().catch(e => console.log("Music ready"));
        }, { once: true });

        // The Runaway No Button
        function moveButton() {
            const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
            const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);
            noBtn.style.left = x + 'px';
            noBtn.style.top = y + 'px';
        }

        noBtn.addEventListener('mouseover', moveButton);
        noBtn.addEventListener('touchstart', moveButton);

        // Yes Button Action
        yesBtn.addEventListener('click', () => {
            document.getElementById('content').style.display = 'none';
            document.getElementById('successMessage').style.display = 'block';
            document.body.style.background = "#ffccd5";
        });
    </script>
</body>
</html>
